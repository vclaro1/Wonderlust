!!! 5
%html
%head
  %title Wonderlust
  = stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true 
  = javascript_include_tag 'application', 'data-turbolinks-track' => true 
  = csrf_meta_tags 

%body
  %nav.navbar.navbar-default
    .container-fluid
      .navbar-header
        %button.navbar-toggle{"data-target" => "#myNavbar", "data-toggle" => "collapse", :type => "button"}
          %span.icon-bar
          %span.icon-bar
          %span.icon-bar
        %a.navbar-brand{:href => "/"}
          %span.glyphicon.glyphicon-flag
          Wanderlust

      - if user_signed_in?
        %ul.nav.navbar-nav.navbar-right
          %li= link_to raw('<span class="glyphicon glyphicon-sunglasses"></span> My Trips'), root_url
          %li= link_to "New Trip", new_trip_path
          %li= link_to raw('<span class="glyphicon glyphicon-search"></span> Search'), new_search_path
          %li= link_to "Account", edit_user_registration_path
          %li= link_to raw('<span class="glyphicon glyphicon-log-out"></span> Log Out'), destroy_user_session_path, method: :delete, confirm: "Are you sure?"
      - else
        %ul.nav.navbar-nav.navbar-right
          %li= link_to raw('<span class="glyphicon glyphicon-user"></span> Register'), new_user_registration_path
          %li= link_to raw('<span class="glyphicon glyphicon-log-in"></span> Login'), new_user_session_path
        
    .container
      
      - flash.each do |name, msg|
        = content_tag :div, msg,  class: "alert alert-info"
      = yield
    / :css
    /   body{padding-top:0px;}
  /     .navbar-brand= link_to "Wonderlust", root_path

  /       - if user_signed_in?
  /         %ul.nav.navbar-nav.navbar-right
  /           %li= link_to "New Trip", new_trip_path
  /           %li= link_to "Account", edit_user_registration_path
  /           %li= link_to "Sign Out", destroy_user_session_path, method: :delete
  /       - else
  /         %ul.nav.navbar-nav.navbar-right
  /           %li= link_to "Sign Up", new_user_registration_path
  /           %li= link_to "Sign In", new_user_session_path
  / .container
  /   - flash.each do |name, msg|
  /     = content_tag :div, msg,  class: "alert alert-info"

  /   = yield



